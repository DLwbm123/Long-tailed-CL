# CT2-D bounded recovery R1

2026-09-18: the container PID 1 started again at 04:43:52 UTC. The original detached launcher, timeout and worker are absent; no normal exit or Python failure receipt survived. The underlying host uptime is not the container uptime. Root cause of the container restart is unresolved. Disk has ~2.2 GiB free; there is no observed disk-full error. Current cgroup OOM counters are zero but cannot establish the previous container's OOM history.

Eight final models completed. ISIC/1994/U retains full post train/val caches and Q00/Q10/Q01/Q11 scores, but has no completed oracle receipt. The remaining fixed parents are ISIC/1994/R, ISIC/1995/U and ISIC/1995/R. All four geometry transitions and eight gradient states are already recorded. No performance-dependent selection was made.

Recovery imports the original qualified worker unchanged, with a separate source binding. It verifies the original parent and manifest hashes, freezes retained scores and cache hashes, and checks the recovered four W/logit arrays against those already saved before accepting the cache. Existing score bytes are never overwritten. The incomplete oracle unit is recalculated on those caches; no image is reread for it. Only the final three parents require new extraction. Temporary caches are removed only after the corresponding unit passes. No original CT1 asset is modified; no test access or training is introduced.

GPU residence is not precisely recoverable across abrupt shutdown. The last recorded 1,586.176 seconds is a lower bound. Admission conservatively charges all elapsed time through the new container start (+2 seconds), plus recorded engineering time: 6,491.861 seconds. The entire unobserved interval after the last ledger is also charged to CPU analytic work (4,897.151 seconds), leaving ~1,650.5 seconds of the separate CPU budget and ~4,308.1 seconds of GPU budget. These are upper-bound accounting values, not measured active utilization. Recovery retains the original 3 GPU-hour / 2 CPU-hour limits.

Four completed ridge solves absent from the last ledger are evidenced by their output files and added back. An independent verification solve and Risk solve could have completed without durable evidence (0–2 additional completed solves). The final report must give a count interval rather than fabricate an exact count. Recalculation is charged as additional work.

Engineering: test_ct2d_recovery.py passed on the existing server runtime: valid retained scores accepted, changed cached features rejected, existing file bytes preserved. Original inference, ridge/Risk, loss, data and report statistics remain locked. This is technical repair attempt 1 of at most 2. No automatic restart after another interruption; no periodic monitoring. Completion requires all 54/837 diagnostic rows and P3 STOP.
