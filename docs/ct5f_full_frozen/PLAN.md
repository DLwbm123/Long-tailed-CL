# CT5-F：完整任务范围的冻结Task1解析参照

独立有限研究；依据CT4-F交付46c372794195ced17fe138328497b85b7d75c83e。用户授权分析后自主设计/执行，并取消GPU小时上限。CT3-P与CT4-F均已STOP，禁止重启或改写。

问题：冻结正常两类Task1末编码器的前缀效果能否保持到全部类别？主比较F1−P，次比较F1−历史U的CT-J-CB（C0），只使用相同val布局。没有全程FD模型，不给FD填最终值，不从前缀外推。

固定范围：原HK23类11任务（2×10+3）、ISIC8类4任务（2×4），原seed/order1993/1994/1995。F1每个序列始终使用自身正常CT1-U Task1末冻结模型，没有独立S0、没有新神经epoch/optimizer step。45个阶段、459逐类行；其中前18阶段预测复用CT4-F已锁定分数，新增27阶段。含P/C0固定对照135阶段/1377逐类行。

原split、类别顺序、权重、预处理、batch48、sample_id排序、pointwise FP32特征、float64 joint归一化/CBRidge、lambda0.001、无bias及1/C类均衡目标不变。六父从CT4-F已验证本地副本只读复用，无新拷贝。继承Task1统计，按真实任务seen边界逐阶段只读当期fit；HK末任务明确为20→23三类。CT4-F没保存可继续的S/mu，所以必须按原逐阶段顺序重建Task2/3统计，声明为本研究的前缀重建提取；不是在线旧类回放。不保存图像特征。前缀W必须与CT4-F锁定W数值相符；不重新产生前缀val预测。

全部45个W与六个网络谱系锁定后，才读取后续各阶段已见类val。全部test/reserved、CT2-D/CT3-P oracle、在线旧fit/未来fit访问0。原CT1/CT3/CT4目录只读。每阶段模型hash不变、样本计数/清单/父SHA严格一致；网络训练入口禁止。失败保留证据，不忽略异常、换模型或重跑取最好。

指标：各阶段BA、accuracy、macroF1、old/current/HM、restricted指标、tail及逐类正确数/分母/component数；全任务平均BA和完整终端BA、最差seed、零召回。主终端F1−P、次终端F1−C0，配对2000次seed49001按类component bootstrap，固定模型、跨seed共享规范样本基础抽样，不重采样seed。原P终端确定性参照不能假装3次独立训练。singleton、多重比较未校正、反复val开发限制保留。

资源/停止：单worker，无GPU小时硬截止，但严格仅上述45阶段；CPU解析与统计报告各不超过30分钟，新磁盘不超过1GiB、运行盘至少空闲1GiB。复用前缀吞吐约821图像/7.28秒作初始资源估计，首次新增阶段再记录吞吐。预计GPU约15–30分钟，按实际进程墙钟报告。不得删除历史资产；完成本研究后STOP，不启动新神经训练、搜索、holdout或更多数据/方法。公开仅源码、配置锁和聚合证据。
