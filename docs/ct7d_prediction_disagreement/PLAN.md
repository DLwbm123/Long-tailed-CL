# CT7-D：固定终端预测分歧诊断

2026-09-20，独立于已STOP的CT6-F。参考交付344d87d22bfa786371d526ae338e4dbdc4746940；独立分支analysis/ct7d-fixed-prediction-disagreement。

问题：FD比正常Task1冻结F1只小幅提升，是因为几乎相同的预测，还是较多纠错被新增错误抵消？T是否改变A的预测？只做已锁定val预测的描述性分析，不训练或构造新分类器。

预定矩阵：HK23类Task11、ISIC8类Task4，三个原seed/order1993/1994/1995。固定比较C3−F1（主）、C2−F1、C3−C2、F1−P。24条配对、372条逐类；另对all/old/current/head/mid/tail六组汇总144行。不选择最佳seed或改变比较。

每样本事件分为双方正确、A纠正B、A破坏B、双方错误但同预测、双方错误且不同预测五个互斥类别。输出类别均衡与样本加权的五类比例、预测一致率、纠错率、破坏率与净差。净差必须复现已有BA/ACC差值；逐类分母及组定义沿用原锁。零事件/空组如实保留。统一argmax复用原classify，标签只在预测生成之后统计正确性，不能进入候选路由。

已知CT6均值不作为新结果；执行前不读取新的样本级分歧。假说待检验，不预设哪种解释成立。共享错误仅说明现有两个决策器未纠正的错误，不证明特征不可分，也不是oracle成绩。禁止选择器、ensemble、温度、阈值、head切换、ridge再拟合或新增训练。已有2000次component配对区间继续作为BA推断依据，本轮不搜索阈值、不新增显著性检验或重抽seed。所有结果是反复使用val后的开发诊断。

输入只允许CT6(C2/C3)、CT5(F1)、CT1(P)终端sealed logits各六份，共24份，以原预测锁记录哈希及映射。提前生成PROTOCOL_LOCK，绑定源脚本、原classify依赖、源预测锁摘要、终端文件和已有聚合指标。工程自检覆盖不均衡权重、纠错/破坏抵消、双方错误分解、标签置换和同分tie。运行必须核对ids/y/order/original/component、样本顺序与BA，拒绝非有限分数、映射差异和锁不一致。

访问旧fit/未来fit/任意图像/特征/W/checkpoint/test/reserved/oracle全部0；新增神经epoch、optimizer step、模型forward和预测全部0。脚本只读白名单npz分数与锁定CSV；文件写入仅新output。私有样本标识和逐样本分歧不公开，仅发布聚合与匿名谱系。

一个CPU worker，GPU=0，CPU进程180秒、内存2GiB、输出10MiB、磁盘空闲至少1GiB；不拉取checkpoint，不需归档服务。用已有hb30128环境与中性/tmp/p26.py入口，保留所有失败和耗时。任一校验失败输出BLOCKED并停止，不自动重复运行。已有CT3/4/5/6不得重启或覆盖。

交付协议锁、工程检查、24/372/144行、净差重现审计、资源与访问账、中文结论及NEXT_DECISION=STOP；公开同分支并匿名验证。后续新训练只能另立有限计划，本轮不自动扩展。
