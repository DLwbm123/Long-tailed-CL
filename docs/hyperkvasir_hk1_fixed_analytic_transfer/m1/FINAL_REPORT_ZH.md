# HK1-M1 当前交付状态

**RUNNING_NOT_COMPLETE**。P0 数据、输入镜像、工程和预算验收通过；2026-09-17 03:48:39 UTC 已在原 hb01 的 RTX 4090 D 启动固定后台管线。启动检查确认主进程及 8 个 loader 子进程正常、日志可用，没有即时失败。正式实验尚未完成，暂无本轮效果结论。

数据：原 outer_train 8,519 / reserved 2,143 的逐样本成员差异为 0；23 类双射与 8 个批准别名通过。10,662 张图像完成 CPU 字节/像素审计，没有精确重复、标签冲突或清理排除。最小合格官方内层 fold 为 3，fit 6,821 / val 1,698，真实 fit 不平衡比 245.3333:1。8,519 张 fit/val 图像的镜像目标 SHA256 与源端一致并可解码，reserved 未复制。

工程：当前 S0 与锁定 V2 原函数的同输入 loss/gradient/update 逐位一致；compact 恢复后的相同下一步更新逐位一致；未来十行 CE 梯度为零、频次索引来自真实 fit 数、训练白名单与优化器分组符合要求。三个初始化的 native B1 / pointwise batch48、反序与换同行检查通过，rtol=atol=1e-5 未放宽。类均衡 ridge 的流式/批量目标、恢复后到达、顺序不变性、重复嵌入和 23 类六阶段指标检查通过。工程更新 8 次，已丢弃，不作为正式父模型。

正式范围：三个新 S0 各 10 epoch，分别 4,062 / 3,045 / 3,539 张 S0 fit 图像，共 850 / 640 / 740 次更新。随后冻结三个父状态，提取固定特征，拟合全部 54 个 A-CB / S-J-CB / S-M-CB 读出并锁定，再统一 val 评价和 2,000 次 component 配对 bootstrap。目标为 54 行主指标、972 行逐类。未引入新训练目标或追加方法。

预算：工程 GPU 驻留 46.05 秒，峰值显存分配约 8.39 GiB。保守 GPU 总驻留估计 3,132.5 秒（52.21 分钟），实验产物峰值估计 469,249,670 bytes（447.51 MiB）；低于 4 小时 / 768 MiB 上限。输入镜像 3,146,187,899 bytes 单列，传输核验后空闲 3,262,185,472 bytes，完整预计输出后仍有超过 1 GiB 余量。

新 test 预测、特征读取和模型前向均为 0；reserved 身份审计文件读取/解码各 2,143 次，明确另计。患者/检查隔离 UNKNOWN；hemorroids 和 ileum 各只有 1 个 val 组件，少数类证据分辨率极低。本数据曾参与历史开发，不是独立确认。

原 BLOCKED 报告、历史权重/数据/checkpoint 和既有修改保留。未恢复小时监测；后台管线包括完整 P1→P2→P3，遇技术失败或预算上限停止并保留证据，完成后 NEXT_DECISION=STOP。本启动快照不能代替最终实验结果。
