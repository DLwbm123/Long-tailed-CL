# HK1-M1 修订应用记录

原 HK1 的 BLOCKED 报告、历史资产和五份既有 CSV 修改全部保留。此目录是用户批准的 HK1-M1 续行，参考 b4a46c022b098e496e2292488a16767eaf28211b，独立分支执行。

已批准的唯一修订：历史 seed1/fold1 的逐样本外层成员不变；官方名称字典序生成项目规范 ID；原数字 order 原样解释于该规范命名空间。只应用 23 行明确映射的 8 个别名，不用规范 ID 重分折，不追加模糊映射。

源端完整身份审计通过后，仅镜像最终 fit/val 到原 hb01 服务器，源图像不动，reserved 不进入模型管线。输入单列但计入总磁盘保留检查；其余实验产物峰值 768 MiB、GPU 累计 4 小时和 CPU 解析 2 小时上限保持。

本文件是修订记录，不是训练已完成声明。P0 及资源验收的实际状态以对应 JSON 为准；完成原固定矩阵后 STOP，不创建监测。
