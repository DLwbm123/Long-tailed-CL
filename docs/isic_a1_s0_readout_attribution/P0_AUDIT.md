# A1 P0/P1 partial audit

**BLOCKED_ROUTING_PARITY.** Asset integrity, three original C-S0 full-network restorations, original-backbone/A-cache parity, A/G1 reproduction, analytic controls and route-ID checks passed. Raw FP32 batch-versus-native-single features failed the unchanged 1e-5 absolute/relative gate for every parent. No S candidate was scored and no S raw cache was created.

The protocol definition existed in commit 6830dab before execution. The post-block protocol/source/view records archive what was attempted; they are explicitly **not a passed release lock**. Parent locks are verified but do not waive the feature gate. Full-matrix throughput admission was not reached. Actual engineering consumption remained within every resource limit.

Initial failure and access/resource snapshots are retained separately. A bounded follow-up used only the predeclared 16 base-train and 16 base-val images per parent; no new candidate, precision, threshold or routing rule was selected. The additional CPU analysis consumed saved A validation scores only.

The two approved pointwise flags do not require renewed permission. A separate numerical-protocol amendment would be a new decision. Current execution is stopped; main, original source-only checkout, historical modifications and all private assets remain preserved.
