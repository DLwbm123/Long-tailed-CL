# A1 来源、实际执行及限制

状态COMPLETE_A1_ATTRIBUTION。完整8变体×3固定父模型/顺序配对×3阶段，共72行主指标、432行逐类（核心36/216）。原A的18个解析解及val分数原位复用，新S拟合54个；未新增神经训练、optimizer step、S0训练或test访问。

实际执行源码为`4c74c4b1e04b396f24c79bbd8819a59bf662d408`，详见CODE_LOCK_A1.json；报告交付提交与执行源码分开记录。依据G1 `8507c2d22916cce7c12c68527eb2515585c0de9e`、V3 `cda1b371225de3568f7f07a8fc1658ec5f03fa3d`、V2交付`a18b25a8c582a0ac0f027bb177dcbdb85ddd7596`及训练源`6d8bd3e700924c864e229c47efe5f108ce728c78`。R1完整网络tensor谱系用于父状态核验，未读取历史test逐样本分数。

A1联合输入、逐样本路由探针、解析目标和矩阵来自本轮批准的计划，不是GSR、ConCM或APART论文规则。直接strict load历史AdapterVitNet全部网络参数/buffers，文件和tensor hash通过。父模型只配自身既有order，未全交叉。只在隔离探针关闭两项batchwise_prompt，保留逐样本top-1。与K同时存在路由、归一化和分类器差异，不能称“只换分类器”。

FP32 Linear数值修复仅使B>1三维输入逐图调用原生算子并拼回原batch。网络仍batch48，native B1未改；权重、公式、精度、路由、1e-5容差不变。三父模型固定布局raw特征均与原生B1逐位一致。首轮阻塞及两项失败控制完整保留，没有按分数选择修复。详见上一级oversight_20260916/REPAIR_REPORT_ZH.md。

沿用V2锁定train 18,718、val 295；训练实际类计数[10529,3263,2835,1306,458,256,44,27]，不平衡比约389.963。标签6的val为26张/24个component，标签7为24张/20个component。未重建split、补样本或重新抽取长尾；标签只用于train解析目标及val评价，不进入encoder forward。每父模型raw main/few只提取一遍，共57,039正式行，派生视图即时构造。

U使用样本均值目标，CB使用类别均值目标；float64统计与求解、lambda=0.001、无bias、无搜索。J整体归一化原始拼接并保留完整交叉Gram；M/F独立归一化。A仅cast既有归一化float32缓存，不重复归一化。统计恢复、复制/重复嵌入、候选类guard、流式等价及同父模型完成后隔离重分块通过。raw缓存、W、身份和逐样本分数私有；聚合与摘要公开，不把统计记忆称为匿名或零记忆。

A最终各为一个确定性参照；S的三个固定父模型/顺序也不是独立患者划分或训练总体。bootstrap为按类分层identity_component的2,000次共享抽样，seed=43001，不重采样父模型。条件性描述区间不能校正反复val开发、历史test反馈、未知患者关联或预训练暴露。

S-J-CB相对A-CB的BA增加4.208 pp，tail增加1.496 pp；tail区间跨0、标签6平均召回下降，存在父模型异质性。固定诊断M-CB优于J-CB，但未替换预定主比较。只支持此探针中的线性可用信息结论，不证明临床有效性、不可逆损坏、因果机制唯一性或外部泛化。可选train重代入评分未生成；train几何和隔离等价检查不视为泛化评价。

原数据、checkpoint、历史失败/负结果、source-only目录及已有修改均保留。独立分支不合并main，未删历史资产、购置算力或下载权重。P0–P3完成即停止；本次用户授权的12小时维护在交付完成后提前暂停，不追加实验。
