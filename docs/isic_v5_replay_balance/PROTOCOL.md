# One equal-group replay-weight contrast on stationary S0 features

V4 F completed all three seeds and improved final validation BA from V3 E's 25.181% to 46.456%. It recovered 31.550 pp old-class recall relative to E, but still lost 16.976 pp against V2 C, exceeding the allowed 5 pp. It did not meet success. Preserve all V4/V3/V2 artifacts.

With the feature space fixed, F's final raw replay losses were 0.5251/0.5340/0.2024. On every final fixed-current-train probe, its weighted replay gradients were smaller than real-objective gradients on both old and current classifier rows. This motivates testing the remaining objective weighting. The probes are local; they neither prove optimal weighting nor rule out Gaussian/augmentation mismatch.

**G is identical to F except that the ConCM-lite replay loss coefficient is 1.0 instead of 0.05.** Choose one predeclared unit coefficient to put the existing real-objective aggregate and old replay loss at equal group coefficients. This is a 20-fold replay increase, not an exactly class-balanced or gradient-balanced objective: the original three real losses, sum/mean reductions, division by 3 and pool weighting remain unchanged. No value is fitted from validation, no parameter grid, no adaptive controller, no new loss or module. Record any loss of current-class plasticity as a negative result.

All non-head parameters remain fixed at the corresponding V2 C S0; preserve original S0 statistics. Reuse the exact 1993/1994/1995 seeds, orders, data manifests and augmentations. G starts from C S0, not from F's trained final state, for a paired comparison. Three trajectories with S1/S2 at 10 epochs each, 60 new epochs, 6 final delta checkpoints, no S0 retraining/early stop/checkpoint selection. Original AdamW head LR .003, empty pool LR .0003, weight decay .01; same batch 48 and synthetic four/old-class, cap 48, var max 1, epsilon 1e-6. New model test predictions: zero.

Primary comparison G−V2 C; mechanism comparison G−V4 F; also report G−V3 E. Same sorted-ID seen-class validation batch layout, all heads/stages/classes/tails and full component logs. Retain the agreed success criterion: positive mean Final BA delta, current recall +10 pp or more, old recall delta at least −5 pp, at least 2/3 paired BA/current improvements, and no added current-class zero recall at S1 or S2. Do not relabel frozen CBRidge reference fits as independent APART improvements.

Reuse the tested S0_HEAD_DELTA_V1 format, immutable parents and normal restore guards. Re-run real-update, frozen-tensor equality, exact restore/continuation and wrong-parent/seed rejection checks. Diagnostic replay gradients must use the actual configured effective replay coefficient (the prior fixed 0.05 was correct for V3/F, but would misreport G). Lock source and configuration before the first formal epoch, measure throughput and peak memory, and reserve output storage. Up to two compatible GPU workers; no total post-V3 GPU-hour cap or hardware purchase. Do not terminate other jobs or delete history to free space.

This separate targeted contrast is covered by the user's autonomous follow-up authorization. Expected duration is roughly the preceding 29-minute F matrix on the same host, subject to a fresh first-epoch resource measurement. New test inference stays prohibited. Report every repeat, including failures, and publish the full validation result before selecting any further intervention. Repeated validation-based experiment selection creates development-set adaptation; no independent test/generalization claim.
